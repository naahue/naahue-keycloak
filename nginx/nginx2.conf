user nginx;
worker_processes auto;

events {
    worker_connections 1024;
}

http {
        sendfile        on;
    server {
        listen 80;
        location / {
            proxy_pass http://demoapp;
        }
    }

    upstream demoapp {
        server demoapp;
    }
}